info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/players/login
  body:
    type: json
    data: |-
      {
        "username": "steve",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
